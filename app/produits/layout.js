export const metadata = {
  title: 'Menu Complet',
  description: 'Explorez notre menu complet de thés, lattes, Bubble Teas, smoothies bols et options santé.',
};

export default function Layout({ children }) {
  return children;
}
